 <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aditya Gupta | Portfolio</title>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet" />

    <style>
        :root {
            --bg: #0f0f0f;
            --text: #e6e6e6;
            --accent: #00eaff;
            --card: #1b1b1b;
            --border: #00eaff;
        }

        body.light {
            --bg: #ffffff;
            --text: #111111;
            --accent: #0077ff;
            --card: #f1f1f1;
            --border: #0077ff;
        }

        body {
            margin: 0;
            font-family: 'Poppins', sans-serif;
            background: var(--bg);
            color: var(--text);
            transition: background 0.4s, color 0.4s;
        }

        * {transition: 0.3s ease;}

        /* NAV */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(0,0,0,0.5);
            backdrop-filter: blur(10px);
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 100;
            border-bottom: 1px solid var(--accent);
        }
        nav a {
            color: var(--text);
            margin: 0 15px;
            text-decoration: none;
        }

        .theme-toggle {
            cursor: pointer;
            padding: 10px 20px;
            border: 1px solid var(--accent);
            border-radius: 25px;
            color: var(--accent);
            background: transparent;
        }

        header {
            padding: 120px 20px 80px;
            text-align: center;
            background: linear-gradient(135deg, #1a1a1a, #3a3a3a);
        }

        h1 {font-size: 55px;margin-bottom: 10px;}
        .tagline {font-size: 20px;color: #cccccc;}

        section {padding: 60px 20px;max-width: 1100px;margin: auto;}
        section h2 {font-size: 34px;text-align: center;margin-bottom: 40px;color: var(--accent);}      

        /* ABOUT */
        .about-container {
            display: flex;
            flex-wrap: wrap;
            gap: 30px;
            align-items: center;
        }
        .about-container img {
            width: 260px;
            border-radius: 20px;
            box-shadow: 0 0 25px rgba(0,255,255,0.4);
        }
        .about-container p {font-size: 18px;line-height: 1.6;}

        /* SKILLS */
        .skill-bar {
            background: var(--card);
            padding: 20px;
            border-radius: 15px;
            margin-bottom: 15px;
            border: 1px solid var(--border);
        }
        .bar {
            height: 12px;
            background: var(--accent);
            width: 0%;
            border-radius: 10px;
            animation: grow 2s forwards;
        }
        @keyframes grow {
            to {width: var(--level);}        }

        /* PROJECTS */
        .project-card {
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 20px;
            margin-bottom: 30px;
            box-shadow: 0 0 15px rgba(0,255,255,0.2);
        }
        .project-card img {
            width: 100%;
            border-radius: 15px;
            margin-bottom: 15px;
        }

        /* RESUME */
        .resume-btn {
            display: block;
            width: 250px;
            margin: 20px auto;
            background: var(--accent);
            color: black;
            padding: 14px;
            border-radius: 40px;
            text-align: center;
            font-size: 18px;
            text-decoration: none;
            font-weight: bold;
        }

        /* CONTACT */
        #contact p {font-size: 20px;text-align: center;}

        footer {
            background: #111;
            text-align: center;
            padding: 20px;
            color: #777;
            margin-top: 40px;
        }
    </style>
</head>

<body>
    <nav>
        <div><strong>Aditya Gupta</strong></div>
        <div>
            <a href="#about">About</a>
            <a href="#skills">Skills</a>
            <a href="#projects">Projects</a>
            <a href="#contact">Contact</a>
        </div>
        <button class="theme-toggle" onclick="toggleTheme()">Toggle Theme</button>
    </nav>

    <header>
        <h1>Aditya</h1>
        <p class="tagline">Web Developer | Designer | Creator</p>
    </header>

    <section id="about">
        <h2>About Me</h2>
        <div class="about-container">
            <img src="https://images.pexels.com/photos/220453/pexels-photo-220453.jpeg" />
            <p>I am a dedicated web developer creating modern, responsive and interactive websites. I enjoy transforming ideas into visually appealing digital products.</p>
        </div>
    </section>

    <section id="skills">
        <h2>Technical Skills</h2>

        <div class="skill-bar"><strong>HTML</strong><div class="bar" style="--level: 95%;"></div></div>
        <div class="skill-bar"><strong>CSS</strong><div class="bar" style="--level: 90%;"></div></div>
        <div class="skill-bar"><strong>JavaScript</strong><div class="bar" style="--level: 85%;"></div></div>
        <div class="skill-bar"><strong>Python</strong><div class="bar" style="--level: 80%;"></div></div>
        <div class="skill-bar"><strong>React.js</strong><div class="bar" style="--level: 75%;"></div></div>
    </section>

    <section id="projects">
        <h2>Projects</h2>

        <div class="project-card">
            <img src="https://images.pexels.com/photos/1181675/pexels-photo-1181675.jpeg" />
            <h3>Portfolio Website</h3>
            <p>A modern animated portfolio website built with HTML, CSS and JavaScript.</p>
        </div>

        <div class="project-card">
            <img src="https://images.pexels.com/photos/3861964/pexels-photo-3861964.jpeg" />
            <h3>Business Dashboard</h3>
            <p>An interactive analytics dashboard built using React.js and APIs.</p>
        </div>

    </section>

    <section id="resume">
        <h2>Resume</h2>
        <a href="resume.pdf" download class="resume-btn">Download My Resume</a>
    </section>

    <section id="contact">
        <h2>Contact Me</h2>
        <p>Email: <strong>aditya@example.com</strong></p>
        <p>Phone: <strong>+91 9876543210</strong></p>
    </section>

    <footer>
        <p>© 2025 Aditya. All Rights Reserved.</p>
    </footer>

    <script>
        function toggleTheme() {
            document.body.classList.toggle('light');
        }
    </script>

</body>
</html>
